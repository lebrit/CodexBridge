# CodexBridge

[English](README.en.md) · Русский

CodexBridge — Windows-приложение с русским GUI для резервного копирования и безопасного восстановления проектов и настроек среды разработки.

Программа предназначена для обычных пользователей Codex, Git и других инструментов. VS Code поддерживается как необязательное дополнение: его карточка показывается, только если редактор найден на компьютере. CodexBridge не привязан к компьютеру автора и работает только с папками и хранилищами, которые выбрал сам пользователь.

## Что уже работает

- быстрый поиск Git- и не-Git-проектов только в выбранных корнях, без полного сканирования дисков;
- поиск и фильтрация каталога, открытие папки проекта и массовое включение или исключение показанных проектов;
- мастер первого запуска, правая навигация и светлая/тёмная тема;
- единый экран состояния защиты с рекомендуемым действием, историей последних операций и индикатором длительной работы;
- основные настройки отделены от раскрываемых облачных, системных и параметров хранения;
- отдельное включение и исключение проектов;
- локальные зашифрованные restic-снимки с автоматическим созданием хранилища при первом backup;
- необязательная вторая копия в любом rclone-совместимом облаке;
- безопасное хранение рабочего ключа через Windows DPAPI;
- фоновый агент и ежечасное задание Планировщика Windows с первым запуском сразу после включения;
- автоматическое обновление каталога перед каждым backup, отметки ручных и фоновых запусков в истории, живое обновление открытого окна и обнаружение устаревшего расписания после смены версии;
- проверка restic-репозитория и просмотр снимков;
- обычная и глубокая проверка части данных репозитория;
- полная пробная выгрузка выбранного снимка с `restic --verify` и точный dry-run add/identical/conflict/skip без изменения рабочих проектов;
- отмена длительных backup, проверок и восстановления с остановкой дочернего процесса;
- необязательная политика хранения старых снимков;
- восстановление проектов в один новый корень;
- политика конфликтов без молчаливой перезаписи;
- транзакционный журнал каждого создаваемого файла, автоматический откат при штатной ошибке или отмене и продолжение отката после аварийного перезапуска;
- проверенный rollback: файл удаляется только при совпадении записанного SHA-256, поэтому последующие пользовательские изменения сохраняются;
- изолированная миграционная лаборатория A→B: настоящий restic-снимок, полная проверка данных, перенос проектов и конфигурации, повторный запуск и сохранение локального конфликта;
- после извлечения снимка Windows ACL и атрибут `ReadOnly` каталогов нормализуются только внутри временного staging-каталога CodexBridge, чтобы данные от другого Windows SID оставались доступны для проверки и безопасной очистки;
- перенос очищенной конфигурации Codex, глобальных AGENTS, rules, memories и пользовательских skills;
- автоматический перенос только разрешённых Git-настроек без credential helper, токенов, signing keys и HTTP-заголовков;
- сохранение реестра Obsidian vault в папку восстановления без подстановки старых машинных путей;
- быстрая диагностика нового компьютера: инструменты, MCP, Graphify, Codebase Memory, Ponytail и пути Obsidian без полного сканирования дисков и вывода секретов;
- подтверждаемая автоподготовка Graphify, Codebase Memory и Ponytail через официальные WinGet/npm/uv/Codex-команды;
- локальная пересборка Graphify и Codebase Memory для защищённых проектов без автоматической публикации в GitHub;
- безопасная перепривязка Obsidian vault по однозначному имени папки: неоднозначные пути не меняются, существующий реестр предварительно сохраняется;
- точный план восстановления WinGet: уже установленные приложения исключаются из импорта, а `--no-upgrade` защищает их версии при повторном запуске;
- сохранение версий Git, Python, Node.js и Java, добавление только существующих разрешённых пользовательских путей и небольшой allowlist переменных без перезаписи конфликтующих значений;
- повторный запуск профиля среды не переустанавливает приложения и расширения VS Code и не дублирует PATH/Git-настройки;
- необязательный перенос настроек, snippets и расширений VS Code;
- встроенный запуск мастера rclone и поиск уже настроенных remotes;
- постоянный `CodexBridge-errors.log` рядом с программой при возникновении ошибок;
- обычный per-user установщик без UAC, обновление поверх предыдущей версии и удаление без удаления `%LOCALAPPDATA%\CodexBridge`;
- автоматические Windows-сборки и версионированные GitHub prerelease с ZIP и установщиком.

## Быстрый старт

1. Скачайте `CodexBridge-...-setup.exe` из раздела **Releases** и запустите установку. ZIP оставлен как переносимый вариант без установки.
2. Откройте CodexBridge из меню «Пуск».
3. Пройдите мастер: выберите единую папку проектов, локальное хранилище и сохраните созданный ключ восстановления.
4. При необходимости нажмите «Установить restic и rclone».
5. Нажмите «Найти проекты сейчас» и проверьте список защищаемых проектов. Поиск обходит только выбранные корни; перед ручным и фоновым backup список обновляется автоматически.
6. Создайте первый backup: пустое локальное хранилище restic будет подготовлено автоматически. Затем проверьте его и включите ежечасное задание.
7. На экране «Восстановление» загрузите снимки, выберите папку проектов и выполните «Проверить и рассчитать изменения» до реального переноса на новый компьютер.
8. После восстановления журнал остаётся в приложении. Если Windows или программа были аварийно закрыты, выберите незавершённую операцию и нажмите «Откатить выбранную операцию».
9. Для облака откройте мастер rclone из настроек, создайте remote и нажмите «Найти настроенные remotes». Адрес хранилища приложение подставит само.
10. На новом компьютере откройте «Программы»: проверьте готовность и точный план установки, затем примените только недостающие приложения и безопасный профиль среды. При необходимости запустите автоподготовку, пересборку индексов и перепривязку Obsidian. Каждое изменяющее действие требует отдельного подтверждения.

Ключ восстановления нельзя восстановить из репозитория. Без него зашифрованные снимки недоступны.

## Приватность

CodexBridge не отправляет телеметрию и не содержит серверной части. Пути вроде `%USERPROFILE%\Documents` вычисляются на компьютере пользователя и не встроены в публичную сборку. Настройки и каталог проектов хранятся в локальном профиле Windows. При ошибке файл `CodexBridge-errors.log` создаётся рядом с EXE; если папка программы защищена от записи, используется `%LOCALAPPDATA%\CodexBridge`. В GitHub публикуются только универсальные исходники и собранная программа.

По умолчанию из снимков исключаются `.env`, приватные ключи, файлы аутентификации, активная база Codex и пересоздаваемые каталоги зависимостей. Перед снимком приложение создаёт отдельную переносимую копию `config.toml`: значения MCP env/headers и настройки с признаками token/password/secret в неё не попадают. Из пользовательского PATH и разрешённых переменных сохраняются только существующие каталоги внутри известных пользовательских или программных папок; корни заменяются переносимыми токенами. Вход в Codex, GitHub и облачные сервисы после переезда выполняется заново.

Перед каждой публичной сборкой запускается проверка на локальные пути, токены, приватные ключи и машинные отчёты.

Подробные обязательства проекта описаны в [Code signing policy](CODE_SIGNING_POLICY.md), включая политику приватности и роли сопровождения.

## Версии и сборки

- локальная версия хранится в `Directory.Build.props`;
- `scripts/Bump-Version.ps1 -Part patch` увеличивает версию для осмысленного релиза;
- каждый push в `main` или `develop` получает уникальную CI-версию;
- GitHub Actions запускает unit-тесты и обязательную restic-лабораторию A→B, создаёт self-contained x64 ZIP, установщик, SHA-256 и prerelease;
- публикация ждёт отдельных проверок на свежих GitHub runners Windows Server 2022 и 2025: установка опубликованного релиза 0.9.0, обновление, повторная установка и удаление;
- 30 проверок отрисовки охватывают шесть страниц в обеих темах и двух размерах окна, а также три шага мастера; проверяются контраст поля хранилища и отсутствие наложения кнопок мастера. PNG и отчёты сохраняются как артефакты Actions на 14 дней, используются только демонстрационные данные;
- при удалении сохраняются пользовательские данные и одноимённое задание Планировщика, если оно указывает на другой исполняемый файл. GitHub runners не заменяют ручную приёмку на чистой Windows 11.

Актуальная последовательность следующих этапов хранится в [`docs/ROADMAP.md`](docs/ROADMAP.md).

## Ограничения первой версии

- Windows 11 x64;
- подключение к облачному провайдеру выполняется в официальном интерактивном мастере rclone;
- конфигурационные файлы при конфликте сохраняются рядом как конфликт, автоматического TOML merge пока нет;
- приложение не копирует пароли, OAuth-сессии и внутреннюю SQLite-базу Codex;
- после автоподготовки всё равно нужно перезапустить Codex, повторно войти в сервисы и вручную подтвердить доверие к hooks плагинов;
- полное испытание переноса желательно провести на втором компьютере или чистой Windows VM.

Заявка CodexBridge в программу SignPath Foundation не была одобрена: проект пока не достиг требуемого уровня внешней известности и общественного доверия. Это не связано с качеством кода или обнаруженной уязвимостью. Текущие файлы остаются неподписанными, пока и в примечаниях конкретного релиза, и в Authenticode-подписи не указано обратное. Самоподписанный сертификат в публичном релизе не используется. Подробности и критерии повторной заявки — в [Code signing policy](CODE_SIGNING_POLICY.md).

## Участие и доверие

Сообщения об ошибках и предложения принимаются через GitHub Issues. Перед публикацией прочитайте [CONTRIBUTING.md](CONTRIBUTING.md); уязвимости следует сообщать по инструкции из [SECURITY.md](SECURITY.md), а не в публичной задаче. Реальные отзывы, независимые обзоры, участники и проверяемая история выпусков помогут проекту позже повторно обратиться в SignPath Foundation.

## Сборка из исходников

```powershell
winget install --id Microsoft.DotNet.SDK.10 --exact
winget install --id restic.restic --exact
powershell -ExecutionPolicy Bypass -File .\scripts\Build-Release.ps1
```

Результат появится в `artifacts`. ZIP собирается всегда; установщик — если найден Inno Setup 6. Если restic не установлен, локальная сборка предупредит о пропуске миграционной лаборатории; в GitHub Actions restic и установщик обязательны.

## Лицензия

MIT — см. [LICENSE](LICENSE).
